# ColoMind Docker Compose
# Complete stack with PostgreSQL and Redis

services:
  colomind:
    build:
      context: .
      dockerfile: Dockerfile
    image: colomind/colomind:${VERSION:-latest}
    container_name: colomind
    restart: unless-stopped
    ports:
      - '${PORT:-3000}:3000'
    environment:
      - NODE_ENV=production
      - PORT=3000
      # LLM Providers
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - MINIMAX_API_KEY=${MINIMAX_API_KEY:-}
      - MINIMAX_GROUP_ID=${MINIMAX_GROUP_ID:-}
      # Database
      - DATABASE_URL=postgresql://colomind:${DB_PASSWORD:-colomind}@postgres:5432/colomind
      # Redis (for Sentinel distributed mode)
      - REDIS_URL=redis://redis:6379
      # Config
      - LOG_LEVEL=${LOG_LEVEL:-info}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:3000/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - colomind-network
    volumes:
      - colomind-data:/app/data

  postgres:
    image: pgvector/pgvector:pg16
    container_name: colomind-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=colomind
      - POSTGRES_PASSWORD=${DB_PASSWORD:-colomind}
      - POSTGRES_DB=colomind
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./sql/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U colomind']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - colomind-network

  redis:
    image: redis:7-alpine
    container_name: colomind-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - colomind-network

networks:
  colomind-network:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  colomind-data:
