services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: colobot-postgres
    environment:
      POSTGRES_USER: colonies
      POSTGRES_PASSWORD: colonies123
      POSTGRES_DB: colobot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U colonies -d colobot"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
